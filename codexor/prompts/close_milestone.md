# Task: Close Milestone

Close the milestone `{{MILESTONE_NAME}}` in repository `{{REPO_FULL_NAME}}`.

Rules:
- Work only in this repository.
- Use the `gh` CLI to close the milestone.
- Do not make any code changes.

Execution:
1) Use `gh` to find the milestone ID or name.
2) Use `gh api` or `gh` commands to close the milestone.

End with exactly one of these lines:
- `ALL DONE` when fully completed.
- `BREAK ON ERROR` if user input is needed or you are permanently stuck.